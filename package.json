{
  "name": "daily-game",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "prettier": "^3.6.2",
    "tailwindcss": "^4.1.15",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@acme/core": "workspace:^",
    "luxon": "^3.7.2"
  }.
  "scripts": {
    "build": "pnpm -r --filter @acme/core... build && pnpm --filter web build"
  }
}
