{
  "extends": "./tsconfig.base.json",         // or whatever you currently extend; keep it if you have it
  "compilerOptions": {
    "baseUrl": ".",
    "moduleResolution": "Bundler",
    "types": ["vite/client"],
    "paths": {
      "@acme/core": ["../../packages/core/src"]  // <-- IMPORTANT: move it here
    }
  },
  "include": ["src"]
}